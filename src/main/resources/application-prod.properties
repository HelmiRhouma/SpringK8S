# Configuration PRODUCTION (Azure/Kubernetes)
spring.datasource.url=jdbc:mysql://${DB_HOST:mysql}:${DB_PORT:3306}/${DB_NAME:springboot_test}
spring.datasource.username=${DB_USER:helmi_user}
spring.datasource.password=${DB_PASSWORD:Helmi@2025}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Logs minimaux en prod
logging.level.root=WARN
logging.level.com.example.test=INFO

# Performance
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.thymeleaf.cache=true
