apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: springboot-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2  # ← CHANGEMENT ICI (était "default")
  resources:
    requests:
      storage: 5Gi